@page
@model CityDetailsModel

<h2>@Model.City.Name</h2>
<p>País: @Model.City.Country.CountryName</p>

<h3>Propriedades:</h3>

<ul>
    @foreach (var property in Model.City.Properties)
    {
        <li>@property.Name - R$ @property.PricePerNight</li>
    }
</ul>